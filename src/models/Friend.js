export default class Friend {
  constructor({ id, name, phone, birthdate, last_visited, avatar }) {
    this.id = id
    this.name = name
    this.phone = phone
    this.birthdate = birthdate
    this.lastVisited = last_visited
    this.avatar = avatar
  }
}
